# Master Node Configuration

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Security
API_KEY=your-secure-api-key-here
ALLOWED_ORIGINS=*

# Storage Configuration
DATA_DIR=./data
STATE_RETENTION_DAYS=30

# Monitoring Configuration
SLAVE_HEARTBEAT_INTERVAL=30  # seconds
SLAVE_TIMEOUT=60            # seconds to mark slave as inactive
MIN_SLAVES_PER_SERVICE=2    # minimum number of slaves to assign per service
MAX_SERVICES_PER_SLAVE=50   # maximum services to assign to a single slave
